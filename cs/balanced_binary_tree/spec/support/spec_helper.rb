# This is a config file that an RoR project would pick up, but this will not mean anything
# to just a few pure ruby script files as in this case

RSpec.configure do |config|
  config.include Helpers
end